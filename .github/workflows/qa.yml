name: QA

on:
  pull_request:
  push:
    branches: [main]

jobs:
  content-qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Content QA against canonical facts
        run: python3 scripts/qa.py
